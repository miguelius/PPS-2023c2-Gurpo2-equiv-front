

server {
  listen       80;
  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    try_files $uri $uri/ /index.html =404;
  }
  location /api {
    set $pass_url http://$backend_host;
    proxy_pass $pass_url;
  }
}
